#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: prints all single digit numbers of base 10 starting from 0 to 9
 * Return: 0
 */
{
int i;
for (i = 0; i < 10; i++)
{
printf("%i", i);
}
printf("\n");
return (0);
}
